services:
  caddy-init:
    image: ${CADDY_IMAGE:?Set immutable CADDY_IMAGE}
    build: !reset null

  caddy:
    image: ${CADDY_IMAGE:?Set immutable CADDY_IMAGE}
    build: !reset null
    environment:
      CADDY_SITE_SCHEME: https
    security_opt:
      - no-new-privileges:true
    cap_drop: ["ALL"]
    cap_add: ["NET_BIND_SERVICE"]
    deploy:
      resources:
        limits:
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "5"

  web:
    image: ${WEB_IMAGE:?Set immutable WEB_IMAGE}
    build: !reset null
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_DEMO_MODE: "false"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop: ["ALL"]
    deploy:
      resources:
        limits:
          memory: 1G
    logging:
      driver: json-file
      options:
        max-size: 20m
        max-file: "5"

  api:
    image: ${API_IMAGE:?Set immutable API_IMAGE}
    build: !reset null
    environment:
      APP_ENV: production
      JWT_SECRET: ${JWT_SECRET:?Set JWT_SECRET}
      OTP_SECRET: ${OTP_SECRET:?Set OTP_SECRET}
      FIELD_ENCRYPTION_KEY: ${FIELD_ENCRYPTION_KEY:?Set FIELD_ENCRYPTION_KEY}
      INTERNAL_SERVICE_TOKEN: ${INTERNAL_SERVICE_TOKEN:?Set INTERNAL_SERVICE_TOKEN}
      COOKIE_SECRET: ${COOKIE_SECRET:?Set COOKIE_SECRET}
      SESSION_COOKIE_SECURE: "true"
      EXPOSE_LOCAL_OTP: "false"
      SEED_DEMO_DATA: "false"
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL}
      REDIS_URL: ${REDIS_URL:?Set REDIS_URL}
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop: ["ALL"]
    deploy:
      resources:
        limits:
          memory: 2G
    logging:
      driver: json-file
      options:
        max-size: 50m
        max-file: "5"

  worker:
    image: ${API_IMAGE:?Set immutable API_IMAGE}
    build: !reset null
    environment:
      APP_ENV: production
      JWT_SECRET: ${JWT_SECRET:?Set JWT_SECRET}
      OTP_SECRET: ${OTP_SECRET:?Set OTP_SECRET}
      FIELD_ENCRYPTION_KEY: ${FIELD_ENCRYPTION_KEY:?Set FIELD_ENCRYPTION_KEY}
      INTERNAL_SERVICE_TOKEN: ${INTERNAL_SERVICE_TOKEN:?Set INTERNAL_SERVICE_TOKEN}
      COOKIE_SECRET: ${COOKIE_SECRET:?Set COOKIE_SECRET}
      EXPOSE_LOCAL_OTP: "false"
      SEED_DEMO_DATA: "false"
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL}
      REDIS_URL: ${REDIS_URL:?Set REDIS_URL}
    read_only: true
    tmpfs: [/tmp]
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    deploy:
      resources:
        limits:
          memory: 2G

  scheduler:
    image: ${API_IMAGE:?Set immutable API_IMAGE}
    build: !reset null
    environment:
      APP_ENV: production
      JWT_SECRET: ${JWT_SECRET:?Set JWT_SECRET}
      OTP_SECRET: ${OTP_SECRET:?Set OTP_SECRET}
      FIELD_ENCRYPTION_KEY: ${FIELD_ENCRYPTION_KEY:?Set FIELD_ENCRYPTION_KEY}
      INTERNAL_SERVICE_TOKEN: ${INTERNAL_SERVICE_TOKEN:?Set INTERNAL_SERVICE_TOKEN}
      COOKIE_SECRET: ${COOKIE_SECRET:?Set COOKIE_SECRET}
      EXPOSE_LOCAL_OTP: "false"
      SEED_DEMO_DATA: "false"
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL}
      REDIS_URL: ${REDIS_URL:?Set REDIS_URL}
    read_only: true
    tmpfs: [/tmp]
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    deploy:
      resources:
        limits:
          memory: 1G

  migrate:
    image: ${API_IMAGE:?Set immutable API_IMAGE}
    build: !reset null
    environment:
      APP_ENV: production
      FIELD_ENCRYPTION_KEY: ${FIELD_ENCRYPTION_KEY:?Set FIELD_ENCRYPTION_KEY}
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL}
      MIGRATION_DATABASE_URL: ${MIGRATION_DATABASE_URL:?Set MIGRATION_DATABASE_URL}

  postgres:
    image: ${POSTGRES_IMAGE:?Set immutable POSTGRES_IMAGE}
    build: !reset null
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?Set POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:?Set POSTGRES_DB}
      APP_POSTGRES_PASSWORD: ${APP_POSTGRES_PASSWORD:?Set APP_POSTGRES_PASSWORD}
    shm_size: 256mb
    deploy:
      resources:
        limits:
          memory: 4G
    logging:
      driver: json-file
      options:
        max-size: 20m
        max-file: "5"

  redis:
    deploy:
      resources:
        limits:
          memory: 1G
    logging:
      driver: json-file
      options:
        max-size: 20m
        max-file: "5"

  backup:
    image: ${BACKUP_IMAGE:?Set immutable BACKUP_IMAGE}
    build: !reset null
    environment:
      APPLICATION_REVISION: ${DEPLOY_REF:?Set immutable DEPLOY_REF}
      BACKUP_IMAGE_TAG: ${INFRA_IMAGE_TAG:?Set immutable INFRA_IMAGE_TAG}
      BACKUP_IMAGE_REF: ${BACKUP_IMAGE:?Set immutable BACKUP_IMAGE}
    security_opt:
      - no-new-privileges:true
    cap_drop: ["ALL"]
    cap_add: ["CHOWN", "FOWNER", "SETGID", "SETUID"]
    deploy:
      resources:
        limits:
          memory: 1G
    logging:
      driver: json-file
      options:
        max-size: 20m
        max-file: "5"

  restore:
    image: ${BACKUP_IMAGE:?Set immutable BACKUP_IMAGE}
    build: !reset null
    environment:
      APPLICATION_REVISION: ${DEPLOY_REF:?Set immutable DEPLOY_REF}
      BACKUP_IMAGE_TAG: ${INFRA_IMAGE_TAG:?Set immutable INFRA_IMAGE_TAG}
      BACKUP_IMAGE_REF: ${BACKUP_IMAGE:?Set immutable BACKUP_IMAGE}
    security_opt:
      - no-new-privileges:true
    cap_drop: ["ALL"]
    cap_add: ["CHOWN", "DAC_OVERRIDE", "FOWNER", "SETGID", "SETUID"]
    deploy:
      resources:
        limits:
          memory: 1G
    logging:
      driver: json-file
      options:
        max-size: 20m
        max-file: "5"
