# Build-plan compliance and evidence ledger

## Purpose

This ledger maps `bumpabestie-buildplan.md` to the repository as it exists. It is
an implementation audit, not a replacement specification and not a launch
certificate. A source file proves that code exists; only a passing test or an
environment-specific transcript proves that it works in that environment.

The plan's one-shot definition of done explicitly requires the complete
provider-backed product on the production host. That broad definition is **not yet
met**. The contained application release is live at exact revision
`c0c15443352ab84fde1d2edfde1ed0692ed842f6` and schema
`0015_bumpa_store_context`. [PR 52](https://github.com/makriman/bumpa-rct-app/pull/52)
is included; exact-revision protected-main
[CI 29412671738](https://github.com/makriman/bumpa-rct-app/actions/runs/29412671738)
passed 13/13 jobs and immutable
[image publication 29413085773](https://github.com/makriman/bumpa-rct-app/actions/runs/29413085773)
passed 7/7 jobs. Six release indexes were promoted, including the one-shot backup image;
seven long-running services use the intended application indexes and Redis uses its
separately pinned upstream digest behind the Cloudflare-proxied branded hosts.

Exactly five approved mapped collaborators can use the bounded country-aware web
login with a six-digit provider-free temporary PIN. Apex, admin and research retain
independent authorization boundaries, and acceptance cleanup left no active
challenge or acceptance session. WhatsApp login, test-sender verification and
proactive/daily/weekly delivery are deliberately parked and disabled. Four Bumpa
stores now complete accepted partial at 8/10 analytics datasets plus orders; the
fifth durably completes degraded at 7/10 plus orders because the provider does not
answer `products.overview` inside the scoped policy. Five isolated Hermes profiles
completed current live Claude canaries. Full Meta activation, the one remaining
provider-side dataset timeout, external alert delivery, formal privacy/security/
retention approval and off-host durability remain open. Raw identities and
credentials remain outside this tracked ledger. The canonical redacted production
transcript is
[`docs/release-evidence-c0c1544.md`](release-evidence-c0c1544.md).

## Status vocabulary

| Status                         | Meaning                                                                                                                                                                              |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `implemented-tested`           | The repository contains the capability and identified automated or local integration evidence has passed. This does not imply live-provider or production proof.                     |
| `production-baseline-disabled` | The capability intentionally fails closed or its service is not started in production because its production dependency is incomplete. This is a safe state, not feature completion. |
| `deferred-provider`            | The live Meta, Bumpa, Hermes/Claude, or related provider integration is intentionally deferred and has no live canary evidence.                                                      |
| `external-blocked`             | Completion depends on infrastructure or credentials outside the repository, such as DNS, SSH authorization, provider accounts, or off-host backup storage.                           |
| `partial`                      | A meaningful subset exists, but one or more build-plan requirements or acceptance tests are missing.                                                                                 |
| `not-implemented`              | No working implementation was found for the stated requirement.                                                                                                                      |

Statuses are deliberately not cumulative. For example, signed webhook handling can
be `implemented-tested` in local mock mode while outbound Meta delivery remains
`deferred-provider` and the production webhook remains
`production-baseline-disabled`.

## Evidence hierarchy

1. `production`: a revision, immutable image digest, timestamp, production command,
   and redacted result.
2. `live`: a provider sandbox/canary transcript tied to a revision.
3. `local`: a reproducible Compose or integration transcript tied to a revision.
4. `contract`: deterministic automated tests and fixtures.
5. `source-only`: implementation presence without execution evidence.

Evidence cannot be promoted across these boundaries. In particular, local mock
tests cannot close live-provider rows, and an HTTP 200 readiness response does not
prove provider reachability unless the readiness implementation actually probes the
provider.

## Temporary web-login release (production verified)

Application release `c0c15443352ab84fde1d2edfde1ed0692ed842f6` parks WhatsApp and provides a
bounded web-only authentication bridge. Its production claim is limited to the
exact revision, schema, immutable images, runtime configuration and redacted
canaries below; it is not a provider-launch certificate.

| Release scope               | Status                         | Repository/contract evidence                                                                                                                                                                                                                                                                                                                                                            | Production evidence                                                                                                                                                                      |
| --------------------------- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Mapped-only temporary login | `implemented-tested`           | The selected temporary mode creates a provider-free, expiring challenge only for an active approved non-opted-out phone/membership/tenant mapping. Generic responses, single use, attempt limits, privacy-preserving Redis limits, audit pseudonyms and session revalidation are covered by focused tests. Migration `0013_web_pin_challenges` constrains concurrent active challenges. | Production contains exactly five approved mappings. Redacted mapped, unmapped and wrong-PIN canaries passed; cleanup left zero active challenges and zero active acceptance sessions.    |
| PIN secret and containment  | `implemented-tested`           | The root-only setter derives a verifier, a networkless initializer makes it API-only and read-only at runtime, production validation requires a future expiry, and the disabled mode remains a kill switch. The raw credential and derived value are absent from this ledger.                                                                                                           | The initial auth rollout's guarded two-phase promotion validated the secret boundary; the current application release reused the active boundary through the normal guarded coordinator. |
| Country-aware web login     | `implemented-tested`           | The form provides an accessible country selector, national-number normalization, E.164 API submission, six-digit entry and provider-free copy. Login/phone/backend-proxy tests cover valid and invalid input states.                                                                                                                                                                    | Current desktop/mobile IAB checks cover the public homepage/login and selector without overflow; the separate 15-cell BFF matrix reached all apex/admin/research destinations.           |
| Platform access             | `implemented-tested`           | Authentication remains independent from tenant and platform authorization. A superadmin-only audited lifecycle manages `operator` and `researcher`; `superadmin` remains protected. Host-only cookies and host-aware destinations retain role denial.                                                                                                                                   | Apex chat, admin and research canaries respected membership/operator/researcher gates. Cookies stayed host-only, and logout revoked tested sessions.                                     |
| WhatsApp parking            | `production-baseline-disabled` | Production validation couples temporary web login to disabled WhatsApp login, test-sender verification and proactive/daily/weekly delivery. Provider secrets remain within their existing scoped boundary for later reviewed activation.                                                                                                                                                | Readiness reported WhatsApp disabled throughout current acceptance. No send or receipt is claimed.                                                                                       |

The complete threat model and operating procedure are in
[`docs/temporary-web-login.md`](temporary-web-login.md). Production evidence must
remain redacted and bound to this exact revision and its six immutable indexes.

## Current release closure and residual classification (2026-07-15)

The rows in this section are backed by exact-revision CI and the production
evidence identified below. Current Bumpa and Hermes canaries are scoped live
evidence; they do not activate WhatsApp, erase the fifth store's typed provider
timeout, or close the broad launch definition.

### Capabilities closed in the release

| Build-plan scope                                       | Release status       | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------ | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 10. Tenant isolation                                   | `implemented-tested` | `ResearchEvent` keys and `AgentMessage.external_message_id` uniqueness are tenant-scoped. `alembic/versions/0009_research_instrumentation.py` rehearses upgrade/downgrade/upgrade, refuses an unsafe downgrade after cross-tenant identifier reuse, and `tests/test_research_instrumentation.py` proves two stores may reuse a web client ID while a same-store replay is deduplicated.                                                                                                                                                                                                                                                                                               |
| 16.1–16.4 Research instrumentation and portal measures | `implemented-tested` | `services/research_events.py` is the consent-gated, idempotent, bounded write boundary. Chat, Hermes, Bumpa, WhatsApp, reports and central admin audit call it. The payload includes `raw_text_present`, redacted text, language, taxonomy, confidence, response length/latency, follow-up, business outcome and quality flags. `tests/test_research_instrumentation.py`, `test_research_privacy.py` and `test_research_overview.py` cover the event catalogue, sanitization and the complete consent-safe measurement catalogue.                                                                                                                                                     |
| 16.5 and 25. Reports/exports                           | `implemented-tested` | `services/reports.py` and `routes/research.py` implement the full report-type catalogue, separate report/export inventory, CSV/JSONL/PDF packages, raw-versus-anonymized gates, checksums, corruption rebuild/fail-closed behavior, expiry, deletion and formula safety. Evidence: `tests/test_research_reports_rich.py` and `test_research_privacy.py`.                                                                                                                                                                                                                                                                                                                              |
| 17. Admin console/API                                  | `implemented-tested` | `services/admin_operations.py`, `providers/hermes_control.py`, `routes/admin.py` and the admin web components provide tenant operations, confirmed/idempotent sync, bounded provider failure views, signed-safe CSV export and authenticated Hermes restart. Evidence: `tests/test_admin_operations.py`, `test_hermes_control_server.py` and `apps/web/tests/admin-operations.test.tsx`.                                                                                                                                                                                                                                                                                              |
| 18. MCP lifecycle                                      | `implemented-tested` | Migration `0010_mcp_connection_lifecycle`, `routes/mcp_admin.py`, `services/mcp_oauth.py`, `services/mcp_permissions.py` and the user/admin MCP pages implement request, operator approval/rejection, OAuth state/PKCE/callback, encrypted tokens, revoke, allowlisted permissions and explicit write confirmation. Evidence: `tests/test_mcp_lifecycle.py` and `apps/web/tests/mcp-lifecycle.test.tsx`. Live connectors remain disabled without provider credentials.                                                                                                                                                                                                                |
| 18. Profile, session, team and phone lifecycle         | `implemented-tested` | `routes/settings.py`, `routes/auth.py`, `core/security.py`, the profile/WhatsApp settings pages and tenant-safe mutation audits implement profile edit/email clear, revoke-other-sessions, team add/reactivate/remove, phone approve/revoke and inactive-member denial. Evidence: `tests/test_settings_and_tenants.py`, `apps/web/tests/user-live-pages.test.tsx` and `whatsapp-settings.test.tsx`.                                                                                                                                                                                                                                                                                   |
| Proactive insights and operational alerts              | `implemented-tested` | `services/proactive_insights.py`, `services/operational_alerts.py`, scheduler/worker handlers and fail-closed settings implement timezone-aware daily/weekly intent, per-recipient membership/consent/opt-out and freshness revalidation, ambiguous-send fencing, starvation-free durable alert discovery, bounded signed PII-safe webhook delivery, early backup-failure alerting and Hermes health evidence. `tests/test_proactive_insights_and_alerts.py` plus async/host contracts cover calendar slots, privacy gates, send fencing, delivery bounds, idempotent discovery and terminal failures. Provider templates and the production alert destination remain external gates. |

### Residual requirements by release class

| Class                          | Residual requirement                                                                                                                                                                                                                                                                                                                                                                                               | Exit evidence                                                                                                   |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------- |
| `closed for contained release` | Exact-revision merged-main CI passed every required job for the deployed revision.                                                                                                                                                                                                                                                                                                                                 | Main CI 29412671738 passed 13/13 jobs for `c0c15443352ab84fde1d2edfde1ed0692ed842f6`.                           |
| `closed for contained release` | Production migrated through `0015_bumpa_store_context`; exact-release CI passed the migration/RLS contracts and production reconciliation proves the active connection/store-context boundary.                                                                                                                                                                                                                     | Production migration-head, exact-release CI, readiness, reconciliation and redacted authentication transcript.  |
| `closed for contained release` | Publish run 29413085773 passed 7/7 jobs; all six exact indexes were promoted and their revision labels match. Seven long-running application services use the intended promoted indexes, Redis uses its separately pinned upstream digest, and all eight run with seven configured health checks passing plus zero restart/OOM/unhealthy signals at acceptance.                                                    | Exact revision/digests, guarded promotion, runtime inspection and edge smoke transcript.                        |
| `closed for release`           | The sealed synthetic resilience runner passed on the frozen release: authenticated concurrent chat/sync pressure enforced exact Redis budgets, chat replay was idempotent, cross-tenant reads failed closed, PostgreSQL invariants showed no corruption, the 97%-used disk event crossed the sanitizer/signature boundary without network delivery, and 50-event plus Redis/PostgreSQL recovery remained lossless. | Green exact-revision CI/local artifact; live provider and external alert receipts remain separate manual gates. |
| `external/manual approval`     | Current evidence records four accepted-partial Bumpa runs at 8/10 plus orders, one degraded run at 7/10 plus orders because `products.overview` alone times out, five Hermes live completions and 20/20 rejected cross-profile gateway credential attempts. Restore 10/10 Bumpa coverage, then run explicitly authorized WhatsApp routing/delivery.                                                                | A successful fifth-store dataset response and an authorized outbound delivery receipt.                          |
| `external/manual approval`     | WhatsApp is parked and disabled. Complete Meta Business verification and obtain approval for OTP, daily/weekly insight and operational templates; record an opted-in outbound message and delivery receipt before activation.                                                                                                                                                                                      | Meta approval references and redacted production delivery evidence.                                             |
| `external/manual approval`     | Configure an encrypted off-host backup target and a real operational-alert HTTPS destination/signing secret; both are currently absent.                                                                                                                                                                                                                                                                            | Remote backup object/checksum/restore and signed alert receipt.                                                 |
| `external/manual approval`     | Obtain privacy/security/retention approval for permissioned raw access, report retention and research consent policy.                                                                                                                                                                                                                                                                                              | Versioned approved policy and named reviewer.                                                                   |
| `temporary-auth follow-up`     | The shared web PIN is a time-bounded bridge, not per-user possession proof. Rotate/expire it under the runbook and replace it before broad or long-lived user access.                                                                                                                                                                                                                                              | Reviewed credential rotation plus a production per-user authentication design and migration canary.             |
| `post-launch`                  | Enable optional Google/Meta MCP OAuth credentials and real connector execution only when those integrations are selected; keep them disabled meanwhile.                                                                                                                                                                                                                                                            | Provider sandbox canaries, token rotation/revocation and per-tool execution audits.                             |
| `post-launch`                  | Optional DOCX export, native UUID/CITEXT migration, GitHub-to-Droplet continuous deployment and deeper product analytics may follow the stable launch without weakening current safety boundaries.                                                                                                                                                                                                                 | Separate approved work items and regression evidence.                                                           |

## Release boundary at this revision

| Boundary                     | Status                           | What is true now                                                                                                                                                                                                                                                                                                                                  | What remains before real use                                                                                                                                         |
| ---------------------------- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Local developer product path | `implemented-tested`             | The release includes migrations through `0015_bumpa_store_context`, bounded temporary web-login, durable jobs, local commerce sync, browser chat, operator onboarding, API-backed user/settings/admin/research views, brand/SEO, signed WhatsApp fixtures, reports and resilience drills.                                                         | WhatsApp activation, the remaining provider timeout, external alerts, governance and off-host durability remain separate gates.                                      |
| Production infrastructure    | `implemented-tested`             | Exact application release `c0c15443352ab84fde1d2edfde1ed0692ed842f6` runs Caddy, web, API, worker, scheduler, Hermes, PostgreSQL and Redis behind five Cloudflare-proxied hosts. All intended services use exact indexes; configured health checks, readiness and async heartbeats pass; protected `main`, CI and publication are green.          | Provider-specific completion gates, external alerts and off-host durability remain open.                                                                             |
| Temporary web authentication | `implemented-tested`             | Exactly five approved mapped collaborators can use the country-aware provider-free six-digit PIN bridge. Apex membership and the independent admin/research roles remain distinct, cookies are host-only, negative canaries fail generically, and acceptance cleanup left zero active auth state.                                                 | Rotate/expire the temporary factor and replace it with per-user proof of possession before broad or long-lived access.                                               |
| WhatsApp/Meta                | `production-baseline-disabled`   | WhatsApp login, test-sender verification and proactive/daily/weekly delivery are parked and disabled. Historical read-only account evidence is not activation or outbound-delivery proof; current acceptance sent no WhatsApp message.                                                                                                            | Complete Meta Business Portfolio/template permissions and sender verification, then record an explicitly authorized opted-in delivery and receipt before activation. |
| Hermes/Claude                | `partial; current live evidence` | Five profiles each completed a current live Claude request from a synthetic prompt while normal tenant-scoped redacted context stayed inside Hermes and bodies remained outside evidence. Five same-profile health probes passed, 20 cross-profile gateway credential attempts were rejected and cleanup left zero active sessions.               | Record WhatsApp-to-correct-profile routing/delivery only after activation gates close.                                                                               |
| Bumpa                        | `partial; current live evidence` | Four mapped runs are accepted partial at 8/10 analytics plus orders. The fifth durably finishes degraded at 7/10 plus orders because `products.overview` alone times out. Current raw/metric/canonical data, currency, ranges, redaction and freshness reconcile.                                                                                 | Obtain a successful current response for the fifth store's remaining dataset.                                                                                        |
| Off-host durability          | `external-blocked`               | A current exact-release local format-3 backup and timer evidence exists, but no encrypted remote object, remote checksum or isolated off-host restore is evidenced.                                                                                                                                                                               | Select and credential an encrypted off-host target, alert on both stages, and restore from that target.                                                              |
| Droplet and domains          | `implemented-tested`             | The exact SSH key works for root and the non-root `bumpabestie` deployment user; Ubuntu 24.04, Docker, key-only SSH, UFW, unattended upgrades and swap are verified. Fail2ban and the stale single-IP SSH rule were removed after a recovery-ISO repair; port 22 now remains reachable for dynamic operators while password and keyboard-interactive authentication stay disabled. All five branded records are Cloudflare-proxied with Full (strict), HTTPS enforcement, minimum TLS 1.2/TLS 1.3, canonical `www` redirect and nonce-bearing dynamic-document CSP. | Configure external alert delivery and continue availability monitoring; use a durable VPN/static CIDR if restricting SSH by source, and do not reintroduce a transient operator `/32`. |

## Section-by-section build-plan audit

### Sections 1–8: product, architecture, surfaces, workflow

| Plan lines / section                         | Status               | Repository evidence                                                                                                                                                                                                                                                                                   | Open requirement / exit evidence                                                                                                                |
| -------------------------------------------- | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| 1–21, project identity/target                | `partial`            | The eight-service stack is live on the production host and branded TLS surfaces at exact revision; WhatsApp is deliberately disabled for the bounded web release.                                                                                                                                     | Complete the provider-backed tenant journeys, alerts, privacy approval and off-host durability.                                                 |
| 22–43, 1. Final product definition           | `partial`            | Public/user/admin/research routes, FastAPI control-plane routes, tenant models, local chat/sync/report flow.                                                                                                                                                                                          | End-to-end SME onboarding and all provider-backed paths must work on the Droplet.                                                               |
| 44–63, 2. Request resolution chain           | `partial`            | Auth resolves membership/tenant, chat builds redacted Bumpa context and routes through the selected profile, WhatsApp uses durable jobs, and RLS protects tenant records. Current evidence records five profile health/live completions and 20/20 rejected cross-profile gateway credential attempts. | Prove explicitly authorized WhatsApp routing/delivery for the mapped contexts.                                                                  |
| 64–88, 3. Domain and surface map             | `implemented-tested` | One Next.js app, host-aware middleware, Caddy virtual hosts, FastAPI API/webhook and branded TLS-enabled production hosts. Current BFF acceptance proves all apex/admin/research auth/destination boundaries; live desktop/mobile visual checks cover public homepage/login.                          | Retain role-by-host regression evidence for future releases.                                                                                    |
| 89–147, 4. Deployment architecture           | `implemented-tested` | Caddy, web, API, worker, scheduler, private Hermes, Postgres, Redis, backup, six exact images, internal networks and scoped secrets are represented, contract-tested and deployed at exact revision/digests.                                                                                          | Continue containment, restart and recovery verification for future releases.                                                                    |
| 148–165, 5. Technology choices               | `implemented-tested` | Next.js/TypeScript, FastAPI/Python 3.12, Postgres 16, Redis 7, durable in-house jobs/outbox, Hermes, Compose and Caddy are present. The integration owner approved deterministic ReportLab rendering as the portable worker-safe PDF architecture in `docs/references.md`.                            | Broader production exception/metrics tooling remains optional; capture production report visual evidence.                                       |
| 166–245, 6. One-shot definition of done      | `partial`            | The exact web software/infrastructure release, branded domains, schema/RLS, five-person mapped-login boundary and role-gated web surfaces are evidenced below.                                                                                                                                        | The definition requires provider-backed journeys, external alerting, privacy approval and off-host durability before unrestricted real use.     |
| 246–331, 7. Repository structure             | `partial`            | Monorepo, apps, infra, scripts, tests and docs exist; worker/scheduler currently share the API image.                                                                                                                                                                                                 | Several proposed modules/packages/scripts are absent or flattened. Structure alone is not a blocker where equivalent boundaries are maintained. |
| 332–513, 8. Local workflow and branch policy | `partial`            | `CODEX.md`, locked dependencies, Make targets, PR template, CODEOWNERS, protected `main`, green CI/publish and exact-revision clean-clone bootstrap evidence exist. Codex is not a runtime dependency.                                                                                                | GitHub-to-Droplet automation is intentionally absent; audited manual stable promotion remains the current path.                                 |

### Sections 9–12: schema, tenancy, API, frontend

| Plan lines / section                        | Status               | Repository evidence                                                                                                                                                                                                                                                                                                                                                                                              | Open requirement / exit evidence                                                                                          |
| ------------------------------------------- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| 514–599, 9. Core/auth schema                | `implemented-tested` | Explicit Alembic migration; tenant, user, platform role, membership, phone, OTP, auth-session and consent models; migration runs in CI.                                                                                                                                                                                                                                                                          | Native UUID/CITEXT and the plan's full metadata/check constraint set require a schema decision and migration evidence.    |
| 600–704, 9. Bumpa schema                    | `implemented-tested` | Encrypted connection, store context/revision, sync metadata, raw response, bounded metric snapshots, canonical orders and items are live at schema 0015. Five current runs reconcile 50 raw analytics rows, 50 metric snapshots, complete order pages, currency, redaction and freshness.                                                                                                                        | Approve the raw-data retention window and obtain provider 10/10 coverage.                                                 |
| 705–747, 9. Hermes schema                   | `partial`            | Profile, agent-message and agent-tool-call records exist. Current evidence records five mapped runtime health/live completions, 20/20 rejected cross-profile gateway credential attempts, five persisted inbound/outbound pairs, usage attribution, zero new errors and cleanup.                                                                                                                                 | Retain the live-profile invariant and capture authorized WhatsApp channel routing.                                        |
| 748–775, 9. WhatsApp schema                 | `implemented-tested` | Messages, delivery events and durable webhook-event claim records; uniqueness/dedupe tests.                                                                                                                                                                                                                                                                                                                      | Production queue ownership, retention, and provider reconciliation remain open.                                           |
| 776–813, 9. Research schema                 | `implemented-tested` | Migration `0009_research_instrumentation` adds tenant-scoped idempotency, `raw_text_present`, language, confidence, response metrics, follow-up, outcomes, quality flags and report/export kind. Retention/expiry, raw-access reason, integrity and lifecycle tests pass; it remains in the live migration chain through schema `0015_bumpa_store_context`.                                                      | Retain migration evidence and repeat the PostgreSQL compatibility gate for future schema changes.                         |
| 814–854, 9. Audit/usage schema              | `implemented-tested` | Audit logs receive correlation IDs plus privacy-bounded `/24` or `/48` peer context and a fixed User-Agent taxonomy. Audit/system-error retention windows are validated, indexed by migration `0012`, and drained oldest-first through bounded `FOR UPDATE SKIP LOCKED` jobs with durable continuations.                                                                                                         | Retain cleanup-job and PostgreSQL compatibility evidence for future schema changes.                                       |
| 855–888, 10. Tenant isolation               | `implemented-tested` | Application RBAC plus explicit `ENABLE`/`FORCE ROW LEVEL SECURITY`; local non-owner probes cover tenant tables. Historical predecessor production evidence verified 23/23 tenant tables and 115 non-bypass contexts across 670 rows with zero leakage. Current exact-release CI reran RLS contracts; schema-0015 store-boundary reconciliation is current without relabelling the historical full-catalog audit. | Repeat the complete catalog/application-role negative probe for the current schema when required by the next launch gate. |
| 889–1039, 11. FastAPI modules and route map | `implemented-tested` | Auth, tenants, settings, chat, Bumpa, Hermes, MCP user/admin approval, admin operations, research/report and webhook routers are registered. Public schema routes remain disabled; a redacted deterministic production-shaped OpenAPI artifact, generated TypeScript contract and bidirectional core-model assertions are checked for drift in CI.                                                               | Inspect the promoted release artifact and repeat the drift gate for future route changes.                                 |
| 1040–1120, 12. Next.js pages and middleware | `implemented-tested` | Every listed top-level page path exists, with host/role middleware, same-origin proxy, API-backed production data and honest states. The exact-revision browser gate passed; current live desktop/mobile checks verify public homepage/login overflow and the 15-cell BFF matrix verifies protected destinations.                                                                                                | Automated checks are not full WCAG conformance; retain authenticated visual evidence for future releases.                 |

### Sections 13–19: providers, research, admin, settings, security

| Plan lines / section                      | Status                         | Repository evidence                                                                                                                                                                                                                                                                                                                                                                          | Open requirement / exit evidence                                                                                                                               |
| ----------------------------------------- | ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1121–1210, 13. WhatsApp routing/webhook   | `implemented-tested`           | Raw-body signature validation, challenge verification, durable event claim/job, message dedupe, phone routing, unknown sender handling, delivery events, STOP/START, retries and Redis limits are contract-tested. Historical ingress evidence is not treated as a current sender canary.                                                                                                    | WhatsApp is disabled; complete the reviewed provider activation gates before any new ingress or delivery canary.                                               |
| 1211–1259, 13. Sender/templates           | `production-baseline-disabled` | The sender's template/text policy, idempotency, response bounds and ambiguous-send guard are tested, but the current release disables WhatsApp login, test-sender verification and proactive/daily/weekly delivery. No outbound is claimed.                                                                                                                                                  | Complete business/sender/template permissions and record an explicitly authorized delivery receipt before activation.                                          |
| 1260–1454, 14. Bumpa direct service       | `partial`                      | Direct HTTP adapter, ten analytics datasets plus orders, store context, encryption, Decimal normalization, unavailable/error semantics, bounded policies, redaction and durable reconciliation are live. Current canaries record stores 1–4 at 8/10 plus orders and store 5 at 7/10 plus orders with one typed provider timeout.                                                             | Obtain provider 10/10 evidence; no Bumpa MCP may be introduced.                                                                                                |
| 1455–1616, 15. Hermes integration         | `partial`                      | Pinned image, private authenticated gateways, profile staging, Hermes-only Anthropic secret, typed client, lifecycle, health and isolation contracts are implemented. Current evidence records five live completions and 20/20 rejected cross-profile gateway credential attempts with clean persistence and session cleanup.                                                                | Record explicitly authorized WhatsApp-to-profile routing/delivery and retain backup recovery evidence.                                                         |
| 1617–1767, 16. Research instrumentation   | `implemented-tested`           | The full §16.1 event catalogue and §16.3 payload are written through a consent-gated, tenant-scoped, idempotent sanitizer. The complete overview catalogue, latency/outcome/follow-up/quality evidence, raw-presence truthfulness, privacy-safe timelines and question-only projections are tested, and migration `0009` is live in production.                                              | Complete production portal visual evidence and obtain formal privacy/security/retention approval.                                                              |
| 1768–1808, 16. Reports/exports            | `implemented-tested`           | The full report catalogue, separate report/export inventory, role/reason gates, structured CSV/JSONL/PDF sections/charts, checksums, retry integrity, formula safety, expiry, cleanup, consent invalidation and download authorization are tested. The approved ReportLab architecture decision is recorded in `docs/references.md`.                                                         | Capture production visual evidence for the deployed release.                                                                                                   |
| 1809–1854, 17. Admin console/API          | `implemented-tested`           | Tenant operations, confirmed/idempotent sync trigger, authenticated Hermes restart, provider status/failure views, CSV-safe admin export and mutation audits have backend/web tests. Production web canaries verified that only approved role holders reached the admin surface; login did not grant or mutate platform access.                                                              | Restore complete provider evidence and record WhatsApp routing/delivery only after activation gates close.                                                     |
| 1855–1917, 18. User settings and MCP      | `implemented-tested`           | Profile edit/email clear, revoke-other-sessions, team add/reactivate/remove, phone approve/revoke, Bumpa status, MCP request/admin approval, OAuth PKCE callback, encrypted tokens, revoke, tool permissions and write confirmation are tested.                                                                                                                                              | Provider OAuth credentials and connector canaries are external/optional; keep connectors disabled until selected and approved.                                 |
| 1918–1956, 19. Secrets and PII            | `partial`                      | Production mock rejection, scoped secrets, authenticated v1/v2 field envelopes, bounded old-key rings, rollback-safe v1 rollout, atomic dry-run-first credential rewrapping, secure cookies, CSRF, signatures, deep redaction, purpose-decoupled pseudonym/integrity settings, privacy-bounded audit context, raw-access gates, artifact expiry and draining operational cleanup are tested. | Execute the staged production v2/material rotation only after the dual-reader soak; obtain formal privacy/security/retention approval and off-host operations. |
| 1957–1981, 19. Rate limiting/agent safety | `partial`                      | Redis-backed privacy-preserving phone/IP limits, challenge attempt bounds and separate costly-operation budgets are enforced. Production temporary-login canaries passed mapped and generic denial paths and were cleaned back to zero active auth state. Tenant context reconstruction, redacted compact context, bounded clients and fail-closed behavior remain contract-tested.          | Per-user possession proof, prompt/tool policy expansion and authorized channel-routing/delivery evidence remain.                                               |

### Sections 20–28: runtime, deployment, backup, observability, quality

| Plan lines / section                   | Status               | Repository evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Open requirement / exit evidence                                                                                                                          |
| -------------------------------------- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1982–2169, 20. Compose/Caddy           | `implemented-tested` | The six-image/eight-service contract is deployed; only Caddy publishes ports, data networks are internal, service health is green, and secret/backup boundaries pass production inspection.                                                                                                                                                                                                                                                                                                                      | Continue to exercise containment and recovery drills after real traffic starts.                                                                           |
| 2170–2262, 21. Images/server exclusion | `implemented-tested` | Protected-main publish run 29413085773 passed 7/7 jobs for deployed `c0c15443352ab84fde1d2edfde1ed0692ed842f6`, producing and scanning six exact `linux/amd64` GHCR indexes with provenance/SBOM and matching revision labels.                                                                                                                                                                                                                                                                                   | Retain registry artifacts and repeat the gate for each future code release.                                                                               |
| 2263–2373, 22. DigitalOcean deployment | `partial`            | Exact-digest application release `c0c15443352ab84fde1d2edfde1ed0692ed842f6` is deployed: all eight intended services run, configured health checks pass, accepted restart/OOM/unhealthy counts are zero, schema 0015 is live, Cloudflare DNS/TLS is ready, and protected-main CI/publish are green.                                                                                                                                                                                                              | Complete WhatsApp, the remaining provider dataset, external alerts and off-host durability.                                                               |
| 2374–2410, 23. Backup/restore          | `partial`            | Restricted-capability format-3 local backup, checksum, isolated restore and scheduled-wrapper contracts are implemented. The current release produced a guarded exact-revision local recovery point with five valid checksum entries and a manifest bound to the exact revision, schema, PostgreSQL 16.14 and backup-image digest. No encrypted remote object or off-host restore is claimed.                                                                                                                    | Configure an encrypted off-host target, alert both stages and prove a remote restore.                                                                     |
| 2411–2443, 24. Observability           | `partial`            | JSON request/job logs, correlation IDs, database plus Redis/worker/scheduler readiness, sanitized system-error/admin views, Docker health checks and rotated logs are implemented. Release alert orchestration discovers Bumpa, WhatsApp and Hermes failures without scan starvation, queues idempotently, signs a bounded PII-safe HTTPS envelope, performs Hermes health checks and signals early backup failure. The real disk checker-to-alert sanitizer/signature path has a deterministic near-full drill. | Configure and prove the external alert destination, off-host alert receipt and live provider health canaries; broader metrics/exception tracking remains. |
| 2444–2501, 25. Reports/exports         | `implemented-tested` | Full catalogue CSV/JSONL/PDF packages, structured sections/charts, deterministic parser coverage and the integrity/privacy lifecycle are implemented locally. The ReportLab substitution is approved and documented.                                                                                                                                                                                                                                                                                             | Capture production visual evidence; DOCX is optional.                                                                                                     |
| 2502–2553, 26. Backend tests           | `implemented-tested` | Release-lock validation, Ruff lint/format, strict mypy, migrations and 483 backend tests at 85.75% branch coverage passed for deployed release `c0c15443352ab84fde1d2edfde1ed0692ed842f6` in protected-main CI 29412671738.                                                                                                                                                                                                                                                                                      | Retain exact-revision artifacts and repeat every gate for future code releases.                                                                           |
| 2554–2569, 26. Frontend tests          | `implemented-tested` | Format, lint, typecheck/build, 171 unit/component tests, dependency audit and pinned-Linux browser gates passed for the exact deployed revision in main CI 29412671738. Current live IAB checks cover brand/login overflow and console cleanliness; the separate BFF matrix covers 15 authorized host/account combinations and logout.                                                                                                                                                                           | Retain exact-revision browser/coverage artifacts.                                                                                                         |
| 2570–2584, 26. Load/failure tests      | `implemented-tested` | The sealed synthetic Compose runner exercises authenticated concurrent web chat and durable Bumpa sync through real API/Postgres/Redis/worker paths, exact rate-limit outcomes, same-tenant chat replay, cross-tenant negative reads, 50-event webhook ingress/replay, Redis/Postgres restarts, and a 97%-used disk event through the real sanitized HMAC alert boundary. Its JSON report contains exact HTTP and durable-state invariants without bearer tokens, identities or alert secrets.                   | Exact-release gates are green; live provider and external alert receipts remain separate manual gates.                                                    |
| 2585–2619, 27. Makefile                | `implemented-tested` | Locked install, lint, format, type, unit, E2E, integration, Compose, backup and restore targets exist.                                                                                                                                                                                                                                                                                                                                                                                                           | Production restore must use the production Compose invocation documented in the runbook, not the local convenience target.                                |
| 2620–2664, 28. Deployment workflow     | `partial`            | CI and an immutable GHCR image publication workflow exist.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | No GitHub-to-Droplet deploy workflow/environment exists; manual SSH deployment is the current intended path.                                              |

### Sections 29–33: workstreams, launch, operations, references, secrets

| Plan lines / section               | Status               | Repository evidence                                                                                                                                                                                                                                                                                           | Open requirement / exit evidence                                                                                                           |
| ---------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| 2665–2890, 29. Workstreams         | `partial`            | Repo/infra, database/auth, local provider ports, main pages, quality and docs have substantial implementation.                                                                                                                                                                                                | Workstreams C–H are not production-complete; live acceptance criteria remain open.                                                         |
| 2891–2917, 30. Launch checklist    | `external-blocked`   | The contained release has exact deployed CI/publish, branded TLS, schema/runtime readiness, five mapped logins, brand/SEO, country-aware desktop/mobile canaries, current five-store Bumpa persistence and five-profile Hermes completions. WhatsApp is disabled; Store 5 retains one typed provider timeout. | Complete Meta sender/template/delivery, the remaining provider dataset, off-host restore, alert destination and privacy/security approval. |
| 2918–2970, 31. Operating runbook   | `partial`            | The initial auth rollout exercised the two-phase sequence. The current release exercised the guarded coordinator, immutable runtime verification, web-login canaries, containment controls and an exact-successor guarded backup; local destructive restore remains contract-tested.                          | Record incident destinations and recovery objectives; exercise off-host restore after storage is selected.                                 |
| 2971–3031, 32. References          | `implemented-tested` | `docs/references.md` records the primary links named by the plan and their intended use.                                                                                                                                                                                                                      | Keep live-provider versions and contract snapshots pinned, reviewed and refreshed.                                                         |
| 3032–3157, 33. Secrets/credentials | `partial`            | Examples contain placeholders only; CI scans repository history. Production provider/app credentials are available through scoped host boundaries; Anthropic values are excluded from the shared app environment and tenant Bumpa values remain encrypted/mapped, not global environment values.              | Configure the off-host backup and operational-alert secrets, verify rotation/revocation procedures and never commit secret values.         |

## Acceptance matrices

### Production infrastructure release

The following gates record the deployed web infrastructure. They authorize only
the five-person temporary web boundary described above; they do **not** authorize
WhatsApp or unrestricted provider-backed traffic.

| Gate                    | Required artifact                                                                                                                  | Current status                                                                                                                                                                                                                                                                                                            |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Final revision quality  | CI URL plus JUnit/coverage/Playwright artifacts for the exact SHA                                                                  | Complete — exact-revision main CI 29412671738 passed 13/13 jobs for deployed `c0c15443352ab84fde1d2edfde1ed0692ed842f6`                                                                                                                                                                                                   |
| Immutable images        | Six API/web/Caddy/PostgreSQL/backup/Hermes digests tagged `sha-<full-sha>`, provenance/SBOM and exact-digest vulnerability results | Complete — publish run 29413085773 passed 7/7 jobs; exact references and matching revision-label evidence are in `docs/verification.md`                                                                                                                                                                                   |
| SSH                     | Non-root deploy user accepts the intended key; root key-only policy recorded                                                       | Complete — the intended key and hardened access boundary were verified without recording key material in this ledger                                                                                                                                                                                                      |
| Host hardening          | OS, capacity, firewall, updates, Docker and published-port transcript                                                              | Complete for the hardened live baseline                                                                                                                                                                                                                                                                                   |
| DNS/TLS                 | A/AAAA resolution and certificate checks for public, `www`, API, admin and research                                                | Complete — five Cloudflare-proxied A records, Full (strict), HTTPS enforcement, TLS 1.2 minimum/TLS 1.3, TLS 1.0/1.1 rejected and canonical `www` redirect verified                                                                                                                                                       |
| Production environment  | Production mode, mock/demo rejection, web-login boundary, scoped secrets and disabled WhatsApp delivery                            | Complete for `c0c15443352ab84fde1d2edfde1ed0692ed842f6`; temporary web login is active and WhatsApp login/test-sender/proactive delivery are disabled                                                                                                                                                                     |
| Migration/start         | Migration transcript and healthy eight-service topology on immutable images                                                        | Complete — schema `0015_bumpa_store_context`; eight intended services run, configured health checks pass, and accepted restart/OOM/unhealthy counts are zero                                                                                                                                                              |
| Web authentication      | Five-mapping allowlist, generic negative canaries, role/host matrix, logout and post-canary cleanup                                | Complete — country-aware six-digit provider-free login reached the intended apex/admin/research surfaces; role gates and host-only cookies held; zero active acceptance auth state remained                                                                                                                               |
| Provider ingress canary | Callback challenge/signature/durable processing fail closed and succeed with valid inputs                                          | Historical partial evidence only; WhatsApp is disabled in the current contained release and no outbound receipt is claimed                                                                                                                                                                                                |
| Local backup            | Backup ID, manifest, checksum/parser and timer journal                                                                             | Complete for the local stage — the guarded exact-successor recovery point has six files and five verified checksum entries (four data artifacts plus the manifest); its format-3 manifest binds the release revision, schema, PostgreSQL version and exact backup-image digest. This does not satisfy off-host durability |
| Off-host durability     | Remote object ID/checksum and isolated restore result                                                                              | `external-blocked`                                                                                                                                                                                                                                                                                                        |

### Provider activation and unrestricted-use gates

No backend may move from `disabled` to its live selector merely because credentials
exist. Bumpa and Hermes are currently enabled only within the redacted canary scope
recorded above; the rows below define unrestricted-use completion, not a retroactive
claim that the contained read-only/live-chat canaries did not run.

| Provider           | Must be complete before activation or unrestricted use                                                                                                                                                                                       |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Meta WhatsApp      | Credential validation; versioned sender; approved templates; verified callback; durable queue/outbox; signature/dedupe/rate-limit tests; unknown/STOP behavior; live test recipient and delivery receipt; alert/runbook.                     |
| Bumpa              | Allowlisted direct base URL; encrypted per-tenant key; scope verification; all ten analytics datasets plus paginated orders; bounded retries/limits; canonical/raw reconciliation; nested redaction; live sandbox transcript; alert/runbook. |
| Hermes/Claude      | Pinned Hermes image; private topology; per-tenant profile filesystem/process/auth; Hermes-only Anthropic secret; health/restart; compact redacted context; cross-profile isolation; timeout/budget handling; backup/restore; live canary.    |
| Async jobs/reports | Redis queue, transactional handoff/outbox, idempotency, bounded retries/dead-letter visibility, worker/scheduler health, failure/restart tests, alerts and operator replay policy.                                                           |

## Evidence record template

Add a row to `docs/verification.md` or the protected external evidence system for
every environment claim:

```text
Claim:
Environment: contract | local | live | production
UTC timestamp:
Git revision:
Image digest(s):
Command or test ID:
Redacted result/artifact URL:
Operator/reviewer:
Expiry or revalidation condition:
```

Do not paste access tokens, cookies, phone numbers, raw provider payloads, customer
data, `.env.production`, SSH private keys, or decrypted API keys into this ledger.
