# Build-plan compliance and evidence ledger

## Purpose

This ledger maps `bumpabestie-buildplan.md` to the repository as it exists. It is
an implementation audit, not a replacement specification and not a launch
certificate. A source file proves that code exists; only a passing test or an
environment-specific transcript proves that it works in that environment.

The plan's one-shot definition of done explicitly requires the complete product on
the DigitalOcean Droplet. That definition is **not yet met**. Release
`8f290509668de15eaf3621e3213f4276f85a0a83` and all eight intended services are
deployed with the production selectors enabled at schema `0008_bumpa_dataset_failures`.
Branded DNS/TLS, protected `main`, CI/publish, Meta ingress, provider credentials,
backups and the runtime are evidenced below. The user-designated operator identity
already has the intended dual global-admin/demo-tenant role, and all five
owner/tenant/phone/Bumpa mappings are verified in the production database. Real use
remains gated by provider-backed sync/profile/chat canaries, Meta business
verification and approved templates, a real alert destination and off-host
durability. Raw phone numbers remain outside this tracked ledger.

## Status vocabulary

| Status                         | Meaning                                                                                                                                                                              |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `implemented-tested`           | The repository contains the capability and identified automated or local integration evidence has passed. This does not imply live-provider or production proof.                     |
| `production-baseline-disabled` | The capability intentionally fails closed or its service is not started in production because its production dependency is incomplete. This is a safe state, not feature completion. |
| `deferred-provider`            | The live Meta, Bumpa, Hermes/Claude, or related provider integration is intentionally deferred and has no live canary evidence.                                                      |
| `external-blocked`             | Completion depends on infrastructure or credentials outside the repository, such as DNS, SSH authorization, provider accounts, or off-host backup storage.                           |
| `partial`                      | A meaningful subset exists, but one or more build-plan requirements or acceptance tests are missing.                                                                                 |
| `not-implemented`              | No working implementation was found for the stated requirement.                                                                                                                      |

Statuses are deliberately not cumulative. For example, signed webhook handling can
be `implemented-tested` in local mock mode while outbound Meta delivery remains
`deferred-provider` and the production webhook remains
`production-baseline-disabled`.

## Evidence hierarchy

1. `production`: a revision, immutable image digest, timestamp, production command,
   and redacted result.
2. `live`: a provider sandbox/canary transcript tied to a revision.
3. `local`: a reproducible Compose or integration transcript tied to a revision.
4. `contract`: deterministic automated tests and fixtures.
5. `source-only`: implementation presence without execution evidence.

Evidence cannot be promoted across these boundaries. In particular, local mock
tests cannot close live-provider rows, and an HTTP 200 readiness response does not
prove provider reachability unless the readiness implementation actually probes the
provider.

## Current candidate closure and residual classification (2026-07-13)

The working tree is ahead of the production release identified below. The rows in
this section are candidate-release claims backed by contract/local tests; they do
not claim that migrations `0009`–`0010`, the new APIs, or the updated web surfaces
are live on the Droplet.

### Capabilities closed in the candidate

| Build-plan scope                                       | Candidate status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------ | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 10. Tenant isolation                                   | `implemented-tested` | `ResearchEvent` keys and `AgentMessage.external_message_id` uniqueness are tenant-scoped. `alembic/versions/0009_research_instrumentation.py` rehearses upgrade/downgrade/upgrade, refuses an unsafe downgrade after cross-tenant identifier reuse, and `tests/test_research_instrumentation.py` proves two stores may reuse a web client ID while a same-store replay is deduplicated.                                                                                                                                                                                                                                                                                               |
| 16.1–16.4 Research instrumentation and portal measures | `implemented-tested` | `services/research_events.py` is the consent-gated, idempotent, bounded write boundary. Chat, Hermes, Bumpa, WhatsApp, reports and central admin audit call it. The payload includes `raw_text_present`, redacted text, language, taxonomy, confidence, response length/latency, follow-up, business outcome and quality flags. `tests/test_research_instrumentation.py`, `test_research_privacy.py` and `test_research_overview.py` cover the event catalogue, sanitization and the complete consent-safe measurement catalogue.                                                                                                                                                     |
| 16.5 and 25. Reports/exports                           | `implemented-tested` | `services/reports.py` and `routes/research.py` implement the full report-type catalogue, separate report/export inventory, CSV/JSONL/PDF packages, raw-versus-anonymized gates, checksums, corruption rebuild/fail-closed behavior, expiry, deletion and formula safety. Evidence: `tests/test_research_reports_rich.py` and `test_research_privacy.py`.                                                                                                                                                                                                                                                                                                                              |
| 17. Admin console/API                                  | `implemented-tested` | `services/admin_operations.py`, `providers/hermes_control.py`, `routes/admin.py` and the admin web components provide tenant operations, confirmed/idempotent sync, bounded provider failure views, signed-safe CSV export and authenticated Hermes restart. Evidence: `tests/test_admin_operations.py`, `test_hermes_control_server.py` and `apps/web/tests/admin-operations.test.tsx`.                                                                                                                                                                                                                                                                                              |
| 18. MCP lifecycle                                      | `implemented-tested` | Migration `0010_mcp_connection_lifecycle`, `routes/mcp_admin.py`, `services/mcp_oauth.py`, `services/mcp_permissions.py` and the user/admin MCP pages implement request, operator approval/rejection, OAuth state/PKCE/callback, encrypted tokens, revoke, allowlisted permissions and explicit write confirmation. Evidence: `tests/test_mcp_lifecycle.py` and `apps/web/tests/mcp-lifecycle.test.tsx`. Live connectors remain disabled without provider credentials.                                                                                                                                                                                                                |
| 18. Profile, session, team and phone lifecycle         | `implemented-tested` | `routes/settings.py`, `routes/auth.py`, `core/security.py`, the profile/WhatsApp settings pages and tenant-safe mutation audits implement profile edit/email clear, revoke-other-sessions, team add/reactivate/remove, phone approve/revoke and inactive-member denial. Evidence: `tests/test_settings_and_tenants.py`, `apps/web/tests/user-live-pages.test.tsx` and `whatsapp-settings.test.tsx`.                                                                                                                                                                                                                                                                                   |
| Proactive insights and operational alerts              | `implemented-tested` | `services/proactive_insights.py`, `services/operational_alerts.py`, scheduler/worker handlers and fail-closed settings implement timezone-aware daily/weekly intent, per-recipient membership/consent/opt-out and freshness revalidation, ambiguous-send fencing, starvation-free durable alert discovery, bounded signed PII-safe webhook delivery, early backup-failure alerting and Hermes health evidence. `tests/test_proactive_insights_and_alerts.py` plus async/host contracts cover calendar slots, privacy gates, send fencing, delivery bounds, idempotent discovery and terminal failures. Provider templates and the production alert destination remain external gates. |

### Residual requirements by release class

| Class                      | Residual requirement                                                                                                                                                                                                                                                                                           | Exit evidence                                                                                                                    |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `deploy-blocking`          | Candidate local gates are green: backend lock/Ruff/format/strict mypy and 324 tests at 85.02% branch-aware coverage; frontend Prettier/type/lint, 120 tests, production build and 18 desktop/mobile Playwright checks; host scripts have 37 tests. Run the same matrix and production contracts on the frozen final revision. | Green exact-revision CI/local artifacts with no dirty-tree ambiguity.                                                            |
| `deploy-blocking`          | Rehearse migrations through `0011_tenant_onboarding` and the updated RLS/isolation probes on PostgreSQL 16, then migrate the Droplet from its currently recorded `0008_bumpa_dataset_failures` schema.                                                                                                         | PostgreSQL upgrade/downgrade/upgrade transcript, RLS negative probes, production migration ID and backup ID tied to the release. |
| `deploy-blocking`          | Publish immutable images for the candidate, deploy all eight services, and repeat health, containment, smoke, backup and rollback verification.                                                                                                                                                                | Exact revision/digests and redacted production transcript.                                                                       |
| `deploy-blocking`          | The sealed synthetic resilience runner passed on 2026-07-13: authenticated concurrent chat/sync pressure enforced exact Redis budgets, chat replay was idempotent, cross-tenant reads failed closed, Postgres invariants showed no corruption, the 97%-used disk event crossed the real sanitizer/signature boundary without network delivery, and the 50-event plus Redis/PostgreSQL recovery phases remained lossless. Repeat this runner on the frozen release. | Green uploaded resilience JSON tied to the exact candidate revision.                                                             |
| `external/manual approval` | Re-verify the five existing production mappings through the audited admin flow, then run isolated Bumpa sync, Hermes/profile and WhatsApp/chat canaries for every mapped tenant.                                                                                                                               | Audited tenant/profile/phone mapping inventory plus five per-tenant sync/chat canary records.                                    |
| `external/manual approval` | Complete Meta Business verification and obtain approval for OTP, daily/weekly insight and operational templates; record an opted-in outbound message and delivery receipt.                                                                                                                                     | Meta approval IDs and redacted production delivery evidence.                                                                     |
| `external/manual approval` | Configure an encrypted off-host backup target and a real operational-alert HTTPS destination/signing secret.                                                                                                                                                                                                   | Remote backup object/checksum/restore and signed alert receipt.                                                                  |
| `external/manual approval` | Obtain privacy/retention approval for permissioned raw access, report retention and research consent policy.                                                                                                                                                                                                   | Versioned approved policy and named reviewer.                                                                                    |
| `post-launch`              | Enable optional Google/Meta MCP OAuth credentials and real connector execution only when those integrations are selected; keep them disabled meanwhile.                                                                                                                                                        | Provider sandbox canaries, token rotation/revocation and per-tool execution audits.                                              |
| `post-launch`              | Optional DOCX export, native UUID/CITEXT migration, GitHub-to-Droplet continuous deployment and deeper product analytics may follow the stable launch without weakening current safety boundaries.                                                                                                             | Separate approved work items and regression evidence.                                                                            |

## Release boundary at this revision

| Boundary                     | Status               | What is true now                                                                                                                                                                                                                                                                                                                    | What remains before real use                                                                                                                                         |
| ---------------------------- | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Local developer product path | `implemented-tested` | Compose, migrations through candidate head `0011_tenant_onboarding`, synthetic OTP, durable jobs, local commerce sync, browser chat, resumable operator onboarding, API-backed user/settings/admin/research views, signed WhatsApp fixtures, full research instrumentation/overview, report packages, admin/MCP/P2 lifecycle flows and sealed synthetic resilience drills have local or contract evidence. | The candidate remains undeployed; production provider and exact-revision recovery evidence remains.                                                                  |
| Production infrastructure    | `partial`            | Exact release `8f290509668de15eaf3621e3213f4276f85a0a83` runs Caddy, web, API, worker, scheduler, Hermes, PostgreSQL and Redis on the branded TLS hosts. All eight are healthy; database, Redis and both async heartbeats are ready. Protected `main`, CI and immutable image publication are green.                                | Off-host durability, external alerts, promotion of the candidate changes and provider-specific user-journey gates remain open.                                       |
| WhatsApp/Meta                | `partial`            | The phone is verified, connected to Cloud API and registered. App and WABA `messages` subscriptions match the production callback; valid/invalid challenges and signed public webhook processing pass.                                                                                                                              | Complete Meta Business Portfolio verification, obtain approval for `bb_otp_login` and an operational template, then record opted-in outbound and delivered receipts. |
| Hermes/Claude                | `partial`            | The pinned Hermes image is live and healthy; the Anthropic credential is valid and mounted only at the Hermes boundary. Profile lifecycle, private gateways, staging/reconciliation, client and isolation contracts pass. All five production mappings and the dual global-admin/demo membership are verified in the database.      | Provision or reconcile the mapped profiles and record live Claude, cross-profile, restart and recovery canaries.                                                     |
| Bumpa                        | `partial`            | All five credentials authenticate and all five production mappings are verified in the database. Four credentials completed the bounded provider probe; store 5 returned a degraded timeout with usable prior data. The direct adapter, durable jobs, datasets/orders, redaction and reconciliation are tested.                     | Record a fresh successful store-5 probe and five mapped production sync/reconciliation results.                                                                      |
| Off-host durability          | `external-blocked`   | Pre-promotion format-3 recovery point `20260713T121212Z` records schema `0008_bumpa_dataset_failures`, PostgreSQL 16.14 and the current backup image; five checksums and all archive/dump parsers pass, and the nightly timer is enabled.                                                                                           | Select and credential an encrypted off-host target, alert on both stages, and restore from that target.                                                              |
| Droplet and domains          | `implemented-tested` | The exact SSH key works for root and the non-root deploy user; Ubuntu 24.04, Docker, UFW, fail2ban, unattended upgrades and swap are verified. Branded public, API, admin and research DNS/TLS are complete.                                                                                                                        | Repeat domain smoke checks when the candidate release is promoted.                                                                                                   |

## Section-by-section build-plan audit

### Sections 1–8: product, architecture, surfaces, workflow

| Plan lines / section                         | Status               | Repository evidence                                                                                                                                                                                                                                                        | Open requirement / exit evidence                                                                                                                   |
| -------------------------------------------- | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1–21, project identity/target                | `partial`            | The complete eight-service stack is live on the intended Droplet and branded TLS hosts with Meta, Bumpa and Hermes selected.                                                                                                                                               | Complete the provider-backed tenant journeys, candidate promotion, alerts and off-host durability.                                                 |
| 22–43, 1. Final product definition           | `partial`            | Public/user/admin/research routes, FastAPI control-plane routes, tenant models, local chat/sync/report flow.                                                                                                                                                               | End-to-end SME onboarding and all provider-backed paths must work on the Droplet.                                                                  |
| 44–63, 2. Request resolution chain           | `partial`            | Auth resolves membership/tenant, chat builds redacted Bumpa context and routes through the selected profile, WhatsApp uses durable jobs, and RLS protects tenant records.                                                                                                  | Bind and canary the five production tenant/profile contexts.                                                                                       |
| 64–88, 3. Domain and surface map             | `implemented-tested` | One Next.js app, host-aware middleware, Caddy virtual hosts, FastAPI API/webhook and branded TLS-enabled production hosts.                                                                                                                                                 | Repeat complete role-by-host browser tests for the candidate release.                                                                              |
| 89–147, 4. Deployment architecture           | `implemented-tested` | Caddy, web, API, worker, scheduler, private Hermes, Postgres, Redis, backup, six exact images, internal networks and scoped secrets are represented and contract-tested.                                                                                                   | Deploy and record the exact pending release.                                                                                                       |
| 148–165, 5. Technology choices               | `implemented-tested` | Next.js/TypeScript, FastAPI/Python 3.12, Postgres 16, Redis 7, durable in-house jobs/outbox, Hermes, Compose and Caddy are present. The integration owner approved deterministic ReportLab rendering as the portable worker-safe PDF architecture in `docs/references.md`. | Broader production exception/metrics tooling remains optional; capture candidate report visual evidence after deployment.                          |
| 166–245, 6. One-shot definition of done      | `not-implemented`    | Individual local subsets are evidenced below.                                                                                                                                                                                                                              | The definition requires all capabilities working on the Droplet; provider, infrastructure, and launch gates are open.                              |
| 246–331, 7. Repository structure             | `partial`            | Monorepo, apps, infra, scripts, tests and docs exist; worker/scheduler currently share the API image.                                                                                                                                                                      | Several proposed modules/packages/scripts are absent or flattened. Structure alone is not a blocker where equivalent boundaries are maintained.    |
| 332–513, 8. Local workflow and branch policy | `partial`            | `CODEX.md`, locked dependencies, Make targets, PR template, CODEOWNERS, protected `main` and green CI/publish exist. Codex is not a runtime dependency.                                                                                                                    | Clean-clone bootstrap evidence and a GitHub-to-Droplet deploy workflow are not recorded; audited manual stable promotion remains the current path. |

### Sections 9–12: schema, tenancy, API, frontend

| Plan lines / section                        | Status               | Repository evidence                                                                                                                                                                                                                                                                                                               | Open requirement / exit evidence                                                                                                                            |
| ------------------------------------------- | -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 514–599, 9. Core/auth schema                | `implemented-tested` | Explicit Alembic migration; tenant, user, platform role, membership, phone, OTP, auth-session and consent models; migration runs in CI.                                                                                                                                                                                           | Native UUID/CITEXT and the plan's full metadata/check constraint set require a schema decision and migration evidence.                                      |
| 600–704, 9. Bumpa schema                    | `implemented-tested` | Encrypted connection, sync run/rate-limit metadata, raw response, bounded metric snapshots, complete canonical order fields and order-item model. Migration upgrade/downgrade/upgrade passed on SQLite and Postgres 16.                                                                                                           | Bind evidence to the final SHA; define raw retention and prove live reconciliation.                                                                         |
| 705–747, 9. Hermes schema                   | `partial`            | Profile, agent-message and agent-tool-call records exist. Profile path/port are intentionally nullable only for the mock-to-Hermes transition; the live port has range/uniqueness constraints.                                                                                                                                    | Runtime-backed profile state and tool-call execution/permission evidence must be implemented and tested. Transitional nulls may not qualify a live profile. |
| 748–775, 9. WhatsApp schema                 | `implemented-tested` | Messages, delivery events and durable webhook-event claim records; uniqueness/dedupe tests.                                                                                                                                                                                                                                       | Production queue ownership, retention, and provider reconciliation remain open.                                                                             |
| 776–813, 9. Research schema                 | `implemented-tested` | Migration `0009_research_instrumentation` adds tenant-scoped idempotency, `raw_text_present`, language, confidence, response metrics, follow-up, outcomes, quality flags and report/export kind. Retention/expiry, raw-access reason, integrity and lifecycle tests pass.                                                         | Rehearse on PostgreSQL 16 and deploy the migration.                                                                                                         |
| 814–854, 9. Audit/usage schema              | `partial`            | Audit logs with IP/user-agent fields, system errors with stack and usage events exist.                                                                                                                                                                                                                                            | Validate request-context population and operational retention.                                                                                              |
| 855–888, 10. Tenant isolation               | `implemented-tested` | Application RBAC plus explicit `ENABLE`/`FORCE ROW LEVEL SECURITY`; local non-owner probes covered tenant tables. Research idempotency and web message external-ID uniqueness are tenant-scoped, with cross-tenant reuse and same-tenant replay regressions. A NOSUPERUSER/NOBYPASSRLS tenant-a role returned tenant-a rows only. | Run the updated migration and role/RLS probe on PostgreSQL 16 and the final production revision.                                                            |
| 889–1039, 11. FastAPI modules and route map | `implemented-tested` | Auth, tenants, settings, chat, Bumpa, Hermes, MCP user/admin approval, admin operations, research/report and webhook routers are registered. `test_api_schema_visibility.py` checks visibility/contract boundaries.                                                                                                               | Regenerate and inspect the final production OpenAPI artifact when the candidate is promoted.                                                                |
| 1040–1120, 12. Next.js pages and middleware | `partial`            | Every listed top-level page path exists, with host/role middleware, same-origin proxy, API-backed production data, and honest loading/empty/error/disabled states. Fixtures require explicit labelled demo mode.                                                                                                                  | Complete authenticated browser, keyboard, axe and visual-diff evidence.                                                                                     |

### Sections 13–19: providers, research, admin, settings, security

| Plan lines / section                      | Status               | Repository evidence                                                                                                                                                                                                                                                                                                                                         | Open requirement / exit evidence                                                                                                                          |
| ----------------------------------------- | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1121–1210, 13. WhatsApp routing/webhook   | `implemented-tested` | Raw-body HMAC validation, challenge verification, durable event claim/job, Meta message dedupe, phone routing, unknown sender handling, delivery events, STOP/START, retries and Redis limits are tested.                                                                                                                                                   | A live callback, delivery receipt and load test are required.                                                                                             |
| 1211–1259, 13. Sender/templates           | `partial`            | The Meta sender implements template/text policy, idempotency, response bounds and an ambiguous-send guard; OTP and job tests pass. The production phone is verified/registered; six non-authentication templates were submitted and remain provider-pending.                                                                                                | Obtain template approval and record live OTP/insight/operational message receipts. The authentication template remains blocked by current app permission. |
| 1260–1454, 14. Bumpa direct service       | `partial`            | Direct HTTP adapter, ten analytics datasets plus paginated orders, encryption, Decimal normalization, unavailable/error semantics, limits/retries, deep redaction, durable sync and canonical/raw reconciliation are tested. All five credentials authenticate; four completed the bounded probe and store 5 had a degraded timeout with usable prior data. | Record a fresh successful store-5 probe and all five mapped production tenant syncs. No Bumpa MCP may be introduced.                                      |
| 1455–1616, 15. Hermes integration         | `partial`            | Pinned upstream-derived image, private authenticated gateways, profile staging/reconciliation, Hermes-only Anthropic secret, typed client, lifecycle, health and isolation contracts are implemented. All five database mappings are verified.                                                                                                              | Provision or reconcile the mapped profiles and record Claude, restart, isolation, backup and recovery canaries.                                           |
| 1617–1767, 16. Research instrumentation   | `implemented-tested` | The full §16.1 event catalogue and §16.3 payload are written through a consent-gated, tenant-scoped, idempotent sanitizer. The complete overview catalogue, latency/outcome/follow-up/quality evidence, raw-presence truthfulness, privacy-safe timelines and question-only projections are tested.                                                         | Deploy migration `0009`, validate the production portal and obtain privacy/retention approval.                                                            |
| 1768–1808, 16. Reports/exports            | `implemented-tested` | The full report catalogue, separate report/export inventory, role/reason gates, structured CSV/JSONL/PDF sections/charts, checksums, retry integrity, formula safety, expiry, cleanup, consent invalidation and download authorization are tested. The approved ReportLab architecture decision is recorded in `docs/references.md`.                        | Capture production visual evidence for the deployed candidate.                                                                                            |
| 1809–1854, 17. Admin console/API          | `implemented-tested` | Tenant operations, confirmed/idempotent sync trigger, authenticated Hermes restart, provider status/failure views, CSV-safe admin export and mutation audits have backend and web tests. All five database mappings and the user-designated dual global-admin/demo membership are verified.                                                                 | Record the audited admin/browser and provider-backed sync/profile journeys for all five existing mappings.                                                |
| 1855–1917, 18. User settings and MCP      | `implemented-tested` | Profile edit/email clear, revoke-other-sessions, team add/reactivate/remove, phone approve/revoke, Bumpa status, MCP request/admin approval, OAuth PKCE callback, encrypted tokens, revoke, tool permissions and write confirmation are tested.                                                                                                             | Provider OAuth credentials and connector canaries are external/optional; keep connectors disabled until selected and approved.                            |
| 1918–1956, 19. Secrets and PII            | `partial`            | Production mock rejection, scoped secret files, field encryption, secure cookies, cookie-origin CSRF, webhook signatures, deep redaction, keyed pseudonyms, raw-access reason/audit gates, consent invalidation, artifact expiry and cleanup are tested.                                                                                                    | Versioned key rotation, formal privacy/retention approval, and off-host secret/backup operations remain.                                                  |
| 1957–1981, 19. Rate limiting/agent safety | `partial`            | Redis-backed HMAC phone/IP OTP limits and separate privacy-preserving budgets for web chat, WhatsApp chat, Bumpa sync and research reports are enforced in production. Tenant context reconstruction, redacted compact context, bounded provider clients and fail-closed behavior are tested.                                                               | Prompt/tool policy expansion and production agent canaries remain.                                                                                        |

### Sections 20–28: runtime, deployment, backup, observability, quality

| Plan lines / section                   | Status               | Repository evidence                                                                                                                                                                                                                                                                                                                                                                                                   | Open requirement / exit evidence                                                                                                                        |
| -------------------------------------- | -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1982–2169, 20. Compose/Caddy           | `implemented-tested` | The six-image/eight-service contract is deployed; only Caddy publishes ports, data networks are internal, service health is green, and secret/backup boundaries pass production inspection.                                                                                                                                                                                                                           | Continue to exercise containment and recovery drills after real traffic starts.                                                                         |
| 2170–2262, 21. Images/server exclusion | `implemented-tested` | Protected-main publication for deployed `8f290509668de15eaf3621e3213f4276f85a0a83` produced/scanned the six exact `linux/amd64` GHCR images with provenance/SBOM.                                                                                                                                                                                                                                                     | Repeat publication and scans for the candidate revision.                                                                                                |
| 2263–2373, 22. DigitalOcean deployment | `partial`            | Exact-digest release `8f290509668de15eaf3621e3213f4276f85a0a83` is deployed: eight healthy services, intended readiness, branded DNS/TLS and protected-main CI/publish are green.                                                                                                                                                                                                                                     | Promote the candidate migrations/images and complete provider-backed tenant journeys, alerts and off-host durability.                                   |
| 2374–2410, 23. Backup/restore          | `partial`            | Restricted-capability format-3 recovery point `20260713T121212Z` records pre-promotion revision `f400cfe67628da787f7ee2a3a3f42c78cb6fae3f`, schema `0008_bumpa_dataset_failures`, PostgreSQL 16.14 and the current backup image; five checksums and all archive/dump parsers pass. Isolated restore and the availability-preserving scheduled wrapper pass.                                                           | Configure an encrypted off-host target, alert both stages, and prove a remote restore.                                                                  |
| 2411–2443, 24. Observability           | `partial`            | JSON request/job logs, correlation IDs, database plus Redis/worker/scheduler readiness, sanitized system-error/admin views, Docker health checks and rotated logs are implemented. Candidate alert orchestration discovers Bumpa, WhatsApp and Hermes failures without scan starvation, queues idempotently, signs a bounded PII-safe HTTPS envelope, performs Hermes health checks and signals early backup failure. The real disk checker-to-alert sanitizer/signature path has a deterministic near-full drill. | Configure and prove the external alert destination, off-host alert receipt and live provider health canaries; broader metrics/exception tracking remains. |
| 2444–2501, 25. Reports/exports         | `implemented-tested` | Full catalogue CSV/JSONL/PDF packages, structured sections/charts, deterministic parser coverage and the integrity/privacy lifecycle are implemented locally. The ReportLab substitution is approved and documented.                                                                                                                                                                                                  | Capture production visual evidence; DOCX is optional.                                                                                                   |
| 2502–2553, 26. Backend tests           | `implemented-tested` | Candidate lock validation, Ruff lint/format, strict mypy, migrations through `0011_tenant_onboarding` and 324 tests pass at 85.02% branch-aware coverage, including admin, onboarding, MCP, instrumentation, rich reports, proactive insights/alerts, provider, durable lease, replay, privacy and isolation matrices. Protected-main CI is green for deployed `8f290509668de15eaf3621e3213f4276f85a0a83`. | Bind the candidate gate artifacts to its final revision and rerun in protected-main CI.                                                                 |
| 2554–2569, 26. Frontend tests          | `partial`            | Prettier, lint, typecheck/build, 120 unit/component tests across 21 files and 18 desktop/mobile Playwright checks pass locally; flows include OTP/chat, fail-closed authorization, resumable operator onboarding, team mutation, Bumpa evidence, research filters/report queueing and responsive navigation.                                                                                                      | Bind to final CI, enforce coverage threshold, and add automated axe/visual-diff evidence.                                                               |
| 2570–2584, 26. Load/failure tests      | `implemented-tested` | The sealed synthetic Compose runner exercises authenticated concurrent web chat and durable Bumpa sync through real API/Postgres/Redis/worker paths, exact rate-limit outcomes, same-tenant chat replay, cross-tenant negative reads, 50-event webhook ingress/replay, Redis/Postgres restarts, and a 97%-used disk event through the real sanitized HMAC alert boundary. Its JSON report contains exact HTTP and durable-state invariants without bearer tokens, identities or alert secrets. | Bind the uploaded runner artifact to the frozen candidate revision; live provider and external alert receipts remain separate manual gates.             |
| 2585–2619, 27. Makefile                | `implemented-tested` | Locked install, lint, format, type, unit, E2E, integration, Compose, backup and restore targets exist.                                                                                                                                                                                                                                                                                                                | Production restore must use the production Compose invocation documented in the runbook, not the local convenience target.                              |
| 2620–2664, 28. Deployment workflow     | `partial`            | CI and an immutable GHCR image publication workflow exist.                                                                                                                                                                                                                                                                                                                                                            | No GitHub-to-Droplet deploy workflow/environment exists; manual SSH deployment is the current intended path.                                            |

### Sections 29–33: workstreams, launch, operations, references, secrets

| Plan lines / section               | Status               | Repository evidence                                                                                                                                                                                                                                                                                                             | Open requirement / exit evidence                                                                                                                                                      |
| ---------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2665–2890, 29. Workstreams         | `partial`            | Repo/infra, database/auth, local provider ports, main pages, quality and docs have substantial implementation.                                                                                                                                                                                                                  | Workstreams C–H are not production-complete; live acceptance criteria remain open.                                                                                                    |
| 2891–2917, 30. Launch checklist    | `external-blocked`   | Branded TLS, exact runtime health, Meta signed ingress, all five database mappings, dual global-admin/demo membership, Hermes/Claude credential validation, protected-main CI/publish, production research cleanup and local backup/restore are proven. Four Bumpa probes complete; store 5 is degraded with prior usable data. | Complete a fresh store-5 probe and all five sync/profile/chat journeys; Meta business verification/templates/outbound receipt; off-host restore; alert destination; privacy approval. |
| 2918–2970, 31. Operating runbook   | `partial`            | Baseline diagnostics, deployment verification, negative OTP/docs canaries and production backup/timer operations were exercised on the Droplet; local destructive restore is tested.                                                                                                                                            | Record incident destinations and recovery objectives; exercise off-host restore after storage is selected.                                                                            |
| 2971–3031, 32. References          | `implemented-tested` | `docs/references.md` records the primary links named by the plan and their intended use.                                                                                                                                                                                                                                        | Pin provider versions/contract snapshots when live adapters are implemented.                                                                                                          |
| 3032–3157, 33. Secrets/credentials | `partial`            | Examples contain placeholders only; CI scans repository history. Production provider/app credentials are available through scoped host boundaries; Anthropic values are excluded from the shared app environment and tenant Bumpa values remain encrypted/mapped, not global environment values.                                | Configure the off-host backup and operational-alert secrets, verify rotation/revocation procedures and never commit secret values.                                                    |

## Acceptance matrices

### Production infrastructure release

The following gates record the deployed infrastructure. They do **not** authorize
real users until the provider activation rows and external gates are complete.

| Gate                    | Required artifact                                                                                                                  | Current status                                                                                                                |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| Final revision quality  | CI URL plus JUnit/coverage/Playwright artifacts for the exact SHA                                                                  | Complete for deployed `8f290509668de15eaf3621e3213f4276f85a0a83`; rerun for the candidate                                     |
| Immutable images        | Six API/web/Caddy/PostgreSQL/backup/Hermes digests tagged `sha-<full-sha>`, provenance/SBOM and exact-digest vulnerability results | Complete for deployed `8f290509668de15eaf3621e3213f4276f85a0a83`; repeat publication/scans for the candidate                  |
| SSH                     | Non-root deploy user accepts the exact intended key; root key-only policy recorded                                                 | Complete — fingerprint `SHA256:+n9DH8aIPVN/Rcwqx35jc4+FmoKzDB8/lcaE2222MxQ`                                                   |
| Host hardening          | OS, capacity, firewall, updates, Docker and published-port transcript                                                              | Complete for the hardened live baseline                                                                                       |
| DNS/TLS                 | A/AAAA resolution and certificate checks for public, `www`, API, admin and research                                                | Branded DNS/TLS complete                                                                                                      |
| Production environment  | Mode `production`, mock/demo rejected, providers `meta`/`bumpa`/`hermes`, async runtime true, secret files 0600                    | Complete for `8f290509668de15eaf3621e3213f4276f85a0a83`                                                                       |
| Migration/start         | Migration transcript and healthy eight-service topology on immutable images                                                        | Complete — eight healthy services; schema `0008_bumpa_dataset_failures`. Candidate must advance through `0011_tenant_onboarding`. |
| Provider ingress canary | Callback challenge/signature/durable processing fail closed and succeed with valid inputs                                          | Complete — valid challenge 200/exact body, invalid token 403, signed public webhook processed                                 |
| Local backup            | Backup ID, manifest, checksum/parser and timer journal                                                                             | Complete — `20260713T121212Z`, format 3/five checksums/pre-promotion revision/schema/image verified; timer enabled            |
| Off-host durability     | Remote object ID/checksum and isolated restore result                                                                              | `external-blocked`                                                                                                            |

### Provider activation gates

No backend may move from `disabled` to its live selector merely because credentials
exist.

| Provider           | Must be complete before activation                                                                                                                                                                                                           |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Meta WhatsApp      | Credential validation; versioned sender; approved templates; verified callback; durable queue/outbox; signature/dedupe/rate-limit tests; unknown/STOP behavior; live test recipient and delivery receipt; alert/runbook.                     |
| Bumpa              | Allowlisted direct base URL; encrypted per-tenant key; scope verification; all ten analytics datasets plus paginated orders; bounded retries/limits; canonical/raw reconciliation; nested redaction; live sandbox transcript; alert/runbook. |
| Hermes/Claude      | Pinned Hermes image; private topology; per-tenant profile filesystem/process/auth; Hermes-only Anthropic secret; health/restart; compact redacted context; cross-profile isolation; timeout/budget handling; backup/restore; live canary.    |
| Async jobs/reports | Redis queue, transactional handoff/outbox, idempotency, bounded retries/dead-letter visibility, worker/scheduler health, failure/restart tests, alerts and operator replay policy.                                                           |

## Evidence record template

Add a row to `docs/verification.md` or the protected external evidence system for
every environment claim:

```text
Claim:
Environment: contract | local | live | production
UTC timestamp:
Git revision:
Image digest(s):
Command or test ID:
Redacted result/artifact URL:
Operator/reviewer:
Expiry or revalidation condition:
```

Do not paste access tokens, cookies, phone numbers, raw provider payloads, customer
data, `.env.production`, SSH private keys, or decrypted API keys into this ledger.
